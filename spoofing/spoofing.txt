Pretending to be somebody they are not so that they can gain access to data they should not have access to. For example, an endpoint which accepts an ID could be exposed if somebody changes the id being sent to view somebody's secret data.

In the case of the course's scope, it's mocking a real user token and using that to pretend to be another user.